ejecutar todo lo de pruebas en un try catch, si capturamos una excepcion la devolvemos con 
system.error

crearPlan devuelve un booleano con excepcion si alguna regla no se cumple

crearPlan(plan p) throws CrearPlanEx; //hay que crearla para todas 'nombre''funcion'Ex; hay que heredarlas
excepcion debe ser una interfaz

y todas heredan de ObjetoEx (clase padre) hereda Excepcion

verificar las interfaces

if(plan == null) throw new CrearPlanExcepcion("wtf amigo");